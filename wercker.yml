box: pjvds/scala
build:
  steps:
    - script:
        name: set SBT_OPTS
        code: export SBT_OPTS=""
    - script:
        name: compile
        code: sbt clean compile stage
    - script:
        name: test
        code: sbt test
